

<div class="card-box" style="margin-top: 30px;">
  <h5 style="margin-bottom: 20px;">タグ一覧</h5>
  <ul>
    <li>
      <%= link_to "すべて", posts_path()  %>
    </li>
  <% tag_list.each do |list| %>
    <li>
      <%= link_to list.tag_name, posts_path(tag_id: list.id)  %>
      <%= "(#{list.posts.count})" %>
    </li>
  <% end %>
  </ul>
</div>